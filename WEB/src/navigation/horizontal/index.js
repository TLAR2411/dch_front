// Horizontal nav uses the same part-based menus as the vertical nav.
// See `DefaultLayoutWithHorizontalNav.vue`, which resolves items from
// `@/navigation/vertical/{admin,khmer,english,chinese}` based on the active system part.
export { useNavigation } from '@/navigation/vertical/index.js'
